//  Copyright © 2020 Dicty

struct AWSCredentials {
  let accessKey: String
  let secretKey: String

  static let shared = AWSCredentials()

  private init() {
    accessKey = "{{ argument.accessKey }}"
    secretKey = "{{ argument.secretKey }}"
  }
}
